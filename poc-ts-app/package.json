{
  "name": "bb-credential-poc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "typescript": "^5.8.0"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "start": "node serve.mjs"
  },
  "description": "Local POC for broker credential and access lifecycle management."
}
